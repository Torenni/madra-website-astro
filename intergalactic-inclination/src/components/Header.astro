---
const nav = [
  { label: "About", href: "/about" },
  { label: "Reports", href: "/reports" },
  { label: "Insights", href: "/insights" },
  { label: "Blog", href: "/blog" },
  { label: "Contact", href: "/contact" },
];
---

<header class="border-b bg-white">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-4 py-4">
    <!-- Brand -->
    <a href="/" class="flex items-center gap-4">
      <!-- Real logo (bigger) -->
      <img
        src="/images/logo/madra_logo.svg"
        alt="MADRA logo"
        class="h-14 w-auto md:h-16"
        loading="eager"
        decoding="async"
      />

      <div class="leading-tight">
        <div class="text-base font-semibold text-slate-900 md:text-lg">MADRA</div>
        <div class="text-xs text-slate-500 md:text-sm">
          Managing for Development Results Associates
        </div>
      </div>
    </a>

    <!-- Nav (bigger font like your example) -->
    <nav class="hidden gap-8 md:flex">
      {nav.map((item) => (
        <a
          href={item.href}
          class="text-base font-semibold tracking-wide text-slate-700 hover:text-slate-900"
        >
          {item.label}
        </a>
      ))}
    </nav>

    <!-- CTA -->
    <a
      href="/contact"
      class="hidden rounded-md bg-slate-900 px-4 py-2 text-sm font-semibold text-white hover:bg-slate-800 md:inline-flex"
    >
      Get in touch
    </a>
  </div>
</header>
